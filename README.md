# Flask DevOps Lab again

## Usage
```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

- /api/health will show the response status of the API.
- /api/config will show general information about the API.
- /api/report will show information about the host of the API.
